pyrogram==2.0.0  # Update to the latest Pyrogram version
TgCrypto==1.2.0  # Update TgCrypto to the latest available version
py-tgcalls==2.0.0  # Update py-tgcalls to the latest version
python-dotenv==0.21.0  # Latest version of python-dotenv
requests==2.28.1  # Latest version of requests
aiohttp==3.8.1  # Latest version of aiohttp
aiofiles==0.8.0  # Latest version of aiofiles
asyncio==3.4.3  # asyncio doesn't need to be installed manually, it's part of Python
search_engine_parser==0.6.8  # Update search_engine_parser to the latest available version
ffmpeg-python==0.2.0  # Latest version of ffmpeg-python
ffmpeg==5.1.2  # Latest stable release of FFmpeg
Pillow==9.1.1  # Latest version of Pillow
ujson==5.4.0  # Latest version of ujson
